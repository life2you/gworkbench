[package]
name = "gworkbench-core"
version = "0.1.0"
edition = "2024"
description = "Shared Rust core for the GWorkbench macOS app"

[lib]
crate-type = ["staticlib", "cdylib", "rlib"]

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
